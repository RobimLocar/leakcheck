Estou no meio da implementação de recuperação via SMS (Twilio) no projeto LeakCheck (Next.js, deploy na Vercel, repo em c:\Users\User\OneDrive\Documentos\Meus Apps\leakcheck). Plano completo já aprovado e salvo em `C:\Users\User\.claude\plans\vivid-churning-stallman.md` — leia esse arquivo primeiro pra contexto completo do "porquê" (auditoria de produto identificou SMS/WhatsApp como diferencial real vs concorrentes US-focused, já que a conta Stripe conectada opera no Brasil/LATAM).

## O que já foi feito nesta sessão anterior (já commitado no working tree, não commitado no git ainda)

1. Migração aplicada no Supabase (`tgutecuvrbjzjigljouy`) e arquivo criado: `supabase/migrations/20260620120000_sms_recovery.sql` — adiciona `failed_payments.customer_phone TEXT`.
2. `lib/stripe/fetchFailedPayments.ts` atualizado pra puxar `customer_phone` direto do invoice da Stripe (campo nativo, sem precisar de expand).
3. `lib/sms/client.ts` criado — `sendSms(to, body)` via REST da Twilio (Basic Auth com `TWILIO_ACCOUNT_SID`/`TWILIO_AUTH_TOKEN`, `from: TWILIO_FROM_NUMBER`). Segue o mesmo padrão non-blocking/swallow-errors de `lib/slack/client.ts`. Pula silenciosamente se o telefone não começar com `+` (não adivinha código de país).
4. `app/api/stripe/sync/route.ts` — SMS disparado junto com o e-mail de step 1, quando um pagamento falhado novo é descoberto (gated em `is_pro`).
5. `app/api/cron/process-recovery/route.ts` — SMS disparado junto com os e-mails de step 2 (dia 3) e step 3 (dia 7) da sequência, usando a mesma lógica de `nextEmailStep()`.
6. `app/dashboard/page.tsx` — aba "Email Sequences" virou "Email + SMS Sequences", mostra ícone 📱 quando o cliente tem telefone cadastrado.
7. `npm run build` rodado localmente com sucesso, zero erros de tipo.
8. **Ainda não fiz**: deploy, env vars da Twilio, teste end-to-end real de envio de SMS.

## Bloqueio atual

Pedi ao usuário pra criar conta na Twilio (https://www.twilio.com/try-twilio) e pegar 3 credenciais: `TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN`, e um número de origem (`TWILIO_FROM_NUMBER`). Ele ainda não me passou esses valores nesta conversa anterior — pode ser que ele já tenha as credenciais prontas agora, ou que ainda precise criar a conta. Pergunte direto.

**Atenção de segurança**: NÃO peça pra colar segredos completos no chat se puder evitar — já tivemos um incidente nesta sessão anterior onde uma `sk_live_` da Stripe foi colada em texto puro no chat (ainda pendente de rotação pelo usuário, não esqueça de cobrar isso também). Prefira: usuário cola o valor diretamente no `.env.local` e só confirma os primeiros caracteres aqui, OU se ele colar mesmo assim, trate com cuidado e sugira rotacionar depois.

## O que fazer nesta nova conversa

1. Primeiro, confirme se agora existem ferramentas reais do servidor MCP `twilio-docs` disponíveis (`ToolSearch` com query "twilio"). Esse servidor MCP (`https://mcp.twilio.com/docs`) foi adicionado ao config local nesta sessão anterior, mas só carrega numa conversa nova — é o motivo desta troca de chat. Se houver ferramentas reais de busca de docs, pode usar pra confirmar detalhes da API REST da Twilio (formato de erro, rate limits, etc.) se precisar, mas o código já foi escrito e testado contra o formato documentado publicamente — não é bloqueante.
2. Pergunte ao usuário se ele já tem as 3 credenciais da Twilio prontas.
3. Quando tiver: rode `vercel env add TWILIO_ACCOUNT_SID production` (e `preview`), idem pra `TWILIO_AUTH_TOKEN` e `TWILIO_FROM_NUMBER`, usando `printf '%s' "$VALOR" | npx --yes vercel env add NOME production` (Vercel CLI já está autenticado nesta máquina/projeto — `robimlocar-1655s-projects/leakcheck`, sessão anterior já rodou `vercel link`). Atualize também o `.env.local`.
4. `npm run build` de novo pra garantir, depois `npx --yes vercel deploy --prod --yes`.
5. Teste real: insira uma linha de teste em `failed_payments` via Supabase MCP (`execute_sql`) com um `customer_phone` real (pode usar o número do próprio usuário, em formato E.164 tipo `+55...`) e `failure_reason` qualquer, depois dispare o cron manualmente:
   ```
   curl -s "https://leakcheck-three.vercel.app/api/cron/process-recovery" -H "Authorization: Bearer $CRON_SECRET"
   ```
   (o `CRON_SECRET` está em `.env.local`). Confirme que o SMS chegou de verdade no celular do usuário. Lembre que conta trial da Twilio só manda SMS pra números verificados no console até adicionar cartão — se der erro de "unverified number", oriente o usuário a verificar o número em Console → Phone Numbers → Verified Caller IDs.
6. Apague a linha de teste do banco depois.
7. Quando tudo confirmado, dê um resumo final pro usuário e lembre os 2 pendentes antigos: (a) rotacionar a `sk_live_` da Stripe exposta no chat, (b) considerar corrigir as claims falsas na página `/upgrade` (SOC2, "200+ founders", etc. — flagado numa auditoria anterior, usuário ainda não pediu pra agir).

Arquivos relevantes: `lib/sms/client.ts`, `app/api/stripe/sync/route.ts`, `app/api/cron/process-recovery/route.ts`, `app/dashboard/page.tsx`, `supabase/migrations/20260620120000_sms_recovery.sql`.

Vercel e Supabase MCP já testados e funcionais nesta sessão anterior (não precisa reconectar): team Vercel `team_nYlJd8Xz9lz9N3EhhklZwDtX`, project `prj_dvRqowGtbZXZJC3cZJQAEpJ8m2Vp`; Supabase project `tgutecuvrbjzjigljouy`.
